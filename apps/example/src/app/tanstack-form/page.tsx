import { FormUploader } from '@/components/tanstack-form-uploader';

export default function Page() {
  return (
    <main className="flex h-screen items-center justify-center">
      <FormUploader />
    </main>
  );
}
